# 知识库中文翻译任务 · 交接说明（2026-08-31）

> 用户已把「知识库中文翻译」任务交给新的 AI 接手。本文件是**当前全部状态 + 执行手册**，
> 接手 AI 直接照此执行即可。所有脚本已就绪、数据已清洗干净，只差翻译这一步。
>
> **2026-08-31 下午更新（二期精简）**：用户审视知识库后拍板——删 python-docs/rust-docs
> 两个整源（纯语言 API 文档，与 Linux 教学定位无关，52 万字）+ 清理 Git 导航章节/
> ArchWiki 残渣/同页多变体重复，重建库后 **4539→4024 官方块**。翻译**暂缓**，
> 用户先目验精简后的 knowledge-preview/ 再定翻译范围。

## 1. 任务

把 `.tdsf-data/rag.db` 中全部官方英文知识块（**4024 块**，二期精简后）翻译为简体中文，
写入 `entries.content_zh`，译文同步进 FTS5（中文 query 直接命中译文）。原文 content
保留（RAG 双语检索）。翻译范围（A 全量 / B 核心四类 / C 只哲学）**先与用户确认**，
见 §4。

## 2. 当前状态盘点（2026-08-31 二期精简后实测）

| 项 | 值 |
|---|---|
| 库位置 | `<项目根>/.tdsf-data/rag.db`（**应用只读这个库**——历史双库事故教训，勿动 `sidecar/data/`） |
| 总块数 | 4132（官方 4024 + linux-philosophy 108） |
| content_zh 已翻 | **0**（翻译未开始；用户要求先目验精简效果再定） |
| 分类分布 | services 1499 / cmd-tools 753 / basic-ops 540 / sys-admin 505 / security 398 / net-remote 329 / linux-philosophy 108 |
| 数据形态 | 25 个合并 md（`knowledge-preview/`，7 分类 × ≤5 文件；cmd-tools 删 Python/Rust 后剩 3）→ 按标题边界分块入库，每块 ≤1200 字，块 title = `合并文档名 · 章节标题` |
| 二期清洗 | 删源 2 个（-52 万字）+ Git 导航 4 章 + man 同页变体 3 章 + ArchWiki 残渣/空行 1073 行；脚本 `clean_consolidated.py` 幂等 |
| doc_titles_zh | 已随重建覆盖 21 个合并文件（中文标题+摘要） |
| LLM 配置 | `.tdsf-data/llm_config.json`（deepseek-v4-flash，¥1/M 输入 ¥2/M 输出） |

## 3. 翻译脚本（现成，直接跑）

`src-tauri/sidecar/scripts/translate_knowledge.py`

```bash
cd src-tauri/sidecar
.venv/Scripts/python.exe scripts/translate_knowledge.py --limit 20   # 冒烟
.venv/Scripts/python.exe scripts/translate_knowledge.py              # 全量（后台跑）
# 可选：--source nginx-docs 按源；--force 重翻
```

已实现能力（无需改造）：
- **断点续跑**：content_zh 非空即跳过，中断/失败重跑安全
- **合批优化**：小条目（<1500 字）≤5 条/批拼接翻译，`===zh-sep-N===` 切回，漏段逐条兜底
- **长度校验**：译文 <30% 或 >300% 原文视为失败，重试 2 次
- **LLM 不可用优雅跳过**（logger.warning，不崩）

## 4. 成本与时长（块级口径，4024 官方块，总字符 ~4.23M，二期精简后估算）

| 方案 | 范围 | 成本 | 时长（5条/批 × 1.5min/批） |
|---|---|---|---|
| **A 全量** | 25 文件 7 分类 | ≈¥10 | ~20 小时（~800 批） |
| **B 核心四类** | services+sys-admin+basic-ops+security（2942 块） | ≈¥6.5 | ~13 小时 |
| **C 只哲学** | linux-philosophy 4 篇 | ≈¥0.1 | ~9 分钟 |

（估算按 4539 块旧口径 ¥11.6/22.7h 按块数与字符量等比折算，跑前以冒烟实测为准）

**提速建议**（需用户同意再改）：`_BATCH_MAX_ITEMS` 5→10 或 `_BATCH_MAX_CHARS` 6000→12000，批数减半、时长减半。

## 5. 格式要求（用户钦定，prompt 已内置）

- 保留代码块 / 命令 / 参数原文**不翻译**；保留 markdown 标题结构 / 列表 / GFM 表格
- 技术术语首次出现括注英文：守护进程(daemon)
- 译文 = 简体中文 markdown，排版整洁（标题层级、表格对齐、代码围栏完整）
- 只输出译文正文，不要任何解释

## 6. 执行步骤（建议顺序）

0. **前置关卡（2026-08-31 二期新增）**：用户目验精简后的 `knowledge-preview/`（25 文件），确认删源/清洗无误后再谈翻译范围
1. **先与用户确认翻译范围（A/B/C）**——用户之前的铁律：关键决策先问，不擅自主张
2. 杀应用：`taskkill /F /IM tdsf-terminal-agent.exe`（dev 自动重启就再杀一次；防 KB auto-init 清库重爬）
3. 冒烟 `--limit 20` → 抽查 2-3 块译文（格式/术语/表格保留是否符合 §5）
4. 全量后台跑（非阻塞），尾随日志
5. 完成后 `python scripts/export_knowledge_md.py` 重导出（合并文件含「## 中文译文」段）
6. 验证：content_zh 覆盖率、FTS5 中文 query 冒烟（"如何生成 ssh 密钥"）、语言残留扫描 0、pytest 全绿
7. 重启应用让用户目验 + 按项目规范 git commit / DEV-JOURNAL / dev-state

## 7. 踩坑清单（历史教训，必读）

1. **双库**：只认 `.tdsf-data/rag.db`（main.py 的 TDSF_DATA_DIR 约定）；6 个运维脚本已强制 set 该路径
2. **清库/重建前必须杀应用**：official==0 会触发 sidecar 后台 auto-init 重爬混入爬虫条目
3. **不要在脏数据上翻译**：历史教训——在旧手写 HTML 解析的脏数据上开跑 623 篇翻译，根因修复后全部作废。本轮已是 markdownify 干净数据，且 2026-08-31 二期清洗再加码（删 python/rust 整源 + Git 导航章 + ArchWiki 残渣 + 同页多变体去重），清洗脚本 `--scan` 当前报告 0 残留
4. 中文 query 命中依赖 FTS5 `content_zh_tokens`（jieba）；向量检索为双语 embedding
5. 前端零改动预期：搜索命中显示「合并文档名 · 章节标题」，get_doc 按 url 聚合完整文档——翻译只写 content_zh，不动结构
6. 别擅自主张改结构/范围/格式；不确定先问用户

## 8. 相关文件索引

| 用途 | 脚本 |
|---|---|
| 翻译（本任务） | `src-tauri/sidecar/scripts/translate_knowledge.py` |
| 合并（25 文件映射） | `scripts/consolidate_knowledge.py`（CONSOLIDATED_DOCS 映射表 fail-closed；_EXCLUDED_SOURCES 删源豁免） |
| 清洗垃圾 | `scripts/clean_consolidated.py`（二期：整源排除+Git导航章+ArchWiki残渣+URL去重，幂等） |
| 重建入库 | `scripts/rebuild_from_consolidated.py`（合并 md → 分块 → rag.db） |
| 导出预览 | `scripts/export_knowledge_md.py`（25 文件 + frontmatter，HTML 拒绝校验） |
| 中文标题 | `scripts/gen_titles_zh.py`（doc_titles_zh；重建已用 frontmatter 摘要覆盖，LLM 精修可选） |
| 库读写 | `knowledge/rag.py`（update_content_zh / official_entries / titles_zh） |
| 爬虫转换 | `knowledge/crawlers/html2md.py` + `generic.py`（markdownify GFM） |
