# TDSF Terminal Agent v4.0 — pnpm 配置
# -----------------------------------------------------------------------------
# 参考: https://pnpm.io/settings
# pnpm 11+ 重大变更: .npmrc 只读 auth/registry, 其他配置必须放 pnpm-workspace.yaml
# -----------------------------------------------------------------------------

# 国内镜像
registry=https://registry.npmmirror.com/
