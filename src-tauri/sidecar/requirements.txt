# TDSF Python Sidecar — 核心依赖
# ============================================================================
# Python 3.11+（推荐 3.13，已在 P1-B 启动前校验）
# 安装: pip install -r requirements.txt
# 或:   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ----------------------------------------------------------------------------
# LangGraph — 7 节点 PAOR 监督循环（T-P1-05）
#   - supervisor: 监督节点，路由到其他 6 节点
#   - plan: 规划节点（拆解任务 + 选择 Agent）
#   - act: 执行节点（调用工具 / 子 Agent）
#   - observe: 观察节点（收集结果 + 更新状态）
#   - reflect: 反思节点（评估结果 + 决定下一步）
#   - tool_call: 工具调用节点
#   - permission_check: 权限检查节点
# ----------------------------------------------------------------------------
langgraph>=1.0

# ----------------------------------------------------------------------------
# 网页爬取（knowledge/crawlers 用 BeautifulSoup 解析 HTML）
# ----------------------------------------------------------------------------
beautifulsoup4>=4.12

# LLM 客户端（按需选择，P1-B 默认使用 OpenAI 兼容接口）
#   - langchain-openai: core/llm_config.py 的 _make_openai_call() 依赖
#     `from langchain_openai import ChatOpenAI`；缺失会导致 LLM 不可用
#     （启动日志报 "langchain-openai 未安装"），2026-08-28 换机环境踩坑补录
langchain-openai>=0.3.0

# ----------------------------------------------------------------------------
# 数据验证（Pydantic v2）
#   - 2026-08-28 换机踩坑：tools.rpc_methods（risk/confidence/decision）依赖
#     pydantic，缺失则 sidecar 启动报 "failed to register tools.rpc_methods:
#     No module named 'pydantic'"，AI 工具链静默降级。
# ----------------------------------------------------------------------------
pydantic>=2.0

# ----------------------------------------------------------------------------
# YAML 配置解析
#   - 2026-08-28 换机踩坑：squilla_router / long_context / self_evolution 三个
#     模块 import yaml，缺失则启动报 "No module named 'yaml'"。venv 必须显式装
#     （strands/langchain 的传递依赖不保证带 PyYAML）。
# ----------------------------------------------------------------------------
PyYAML>=6.0

# ----------------------------------------------------------------------------
# 中文分词（knowledge FTS5 检索）
#   - 2026-08-28 换机踩坑：jieba 缺失则 FTS5 降级为空格分词，中文知识库检索
#     质量大幅下降（"关闭防火墙" 检索不到 "iptables/firewalld"）。
# ----------------------------------------------------------------------------
jieba>=0.42

# ----------------------------------------------------------------------------
# SQLite 向量扩展（knowledge RAG 向量检索，可选）
#   - 缺失则向量检索禁用、仅 FTS5 关键词检索（降级可用，不阻塞启动）。
#   - 若当前 Python 版本无预编译 wheel 装不上，可注释此行（FTS5+jieba 仍可用）。
# ----------------------------------------------------------------------------
sqlite-vec>=0.1.0

# ----------------------------------------------------------------------------
# 本地嵌入模型推理（knowledge RAG 语义向量，可选）
#   - fastembed（Qdrant，ONNX）：BGE-small-zh-v1.5 本地推理，零外部 API。
#     缺失时嵌入降级 hash 伪向量（FTS5 仍可用，语义检索无效）。
#   - 模型首次使用需下载（HF_ENDPOINT=https://hf-mirror.com 走国内镜像，
#     缓存于 sidecar/data/models/）；离线环境预下载后即可用。
# ----------------------------------------------------------------------------
fastembed>=0.8.0

# ----------------------------------------------------------------------------
# 向量检索（T-P1-07 ground tool，T-P3-02 ChromaDB）
# ----------------------------------------------------------------------------
chromadb>=0.5.0

# ----------------------------------------------------------------------------
# SQLite（Python 标准库已内置 sqlite3，无需安装）
# 用于 T-P1-03 Project Service 单一写入器（WAL 模式 + 写租约）
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# 异步支持
# ----------------------------------------------------------------------------
asyncio-atexit>=1.0  # Python 3.13 异步清理

# ----------------------------------------------------------------------------
# Strands Agents SDK（TDSF 魔改 2026-07-30 P0-C3 修复）
# -----------------------------------------------------------------------------
# Apache-2.0 协议，AWS 开源 Agent 框架，由 strands_backend/ 适配层封装。
# - 条件依赖：strands_backend/__init__.py try/except ImportError 优雅降级
# - 激活方式：环境变量 TDSF_AGENT_BACKEND=strands 时 main.py 注入适配层
# - 版本约束：1.x 稳定 API（@tool / Agent(tools=...) / stream_async）
#   2.x 可能引入 breaking change，故 <2.0
# - 默认未启用：保持 LangGraph BaseAgent PAOR 主路径，Strands 作为可选后端
# - 安装后还需配置 LLM provider（OpenAI/Anthropic/Bedrock 等），
#   通过 strands_backend.adapter 的 strands_model 参数注入
# -----------------------------------------------------------------------------
strands-agents>=1.0,<2.0

# ----------------------------------------------------------------------------
# 测试（pytest）
# ----------------------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0

# ----------------------------------------------------------------------------
# 开发工具（可选，仅开发环境）
# ----------------------------------------------------------------------------
# ruff>=0.6.0         # Lint + Format
# mypy>=1.10          # 类型检查
# ipython>=8.0        # 交互式调试
