# TDSF Python Sidecar — 核心依赖
# ============================================================================
# Python 3.11+（推荐 3.13，已在 P1-B 启动前校验）
# 安装: pip install -r requirements.txt
# 或:   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ----------------------------------------------------------------------------
# 网页爬取（knowledge/crawlers 用 BeautifulSoup 解析 HTML）
# ----------------------------------------------------------------------------
beautifulsoup4>=4.12

# ----------------------------------------------------------------------------
# HTML → GFM Markdown 转换（knowledge/crawlers/html2md.py，TDSF 2026-08-30 根因修复）
#   - 旧 _extract_page 手动拼 h1/h2/h3+段落纯文本，丢失全部 HTML 结构
#     （<table>→| 文字墙、<ul>/<ol> 粘连、<pre> 无围栏、导航混入）。
#   - markdownify 实测择优：表格→标准 GFM（| --- | 分隔行）、列表→- /1.、
#     代码→``` 围栏、标题→ATX #（html2text 表格缺前导 |、代码是 4 空格
#     缩进，弃用）。
# ----------------------------------------------------------------------------
markdownify>=0.13

# 离线知识库脚本使用的官方同步 LLM 客户端。
# 上限与 Strands 1.x provider 约束保持一致，避免换机安装出不兼容组合。
openai>=1.68,<3
anthropic>=0.21,<1

# ----------------------------------------------------------------------------
# 数据验证（Pydantic v2）
#   - 2026-08-28 换机踩坑：tools.rpc_methods（risk/confidence）依赖
#     pydantic，缺失则 sidecar 启动报 "failed to register tools.rpc_methods:
#     No module named 'pydantic'"，AI 工具链静默降级。
# ----------------------------------------------------------------------------
pydantic>=2.0

# ----------------------------------------------------------------------------
# YAML 配置解析
#   - 2026-08-28 换机踩坑：squilla_router / long_context / self_evolution 三个
#     模块 import yaml，缺失则启动报 "No module named 'yaml'"。venv 必须显式装
#     （第三方框架的传递依赖不保证带 PyYAML）。
# ----------------------------------------------------------------------------
PyYAML>=6.0

# ----------------------------------------------------------------------------
# 中文分词（knowledge FTS5 检索）
#   - 2026-08-28 换机踩坑：jieba 缺失则 FTS5 降级为空格分词，中文知识库检索
#     质量大幅下降（"关闭防火墙" 检索不到 "iptables/firewalld"）。
# ----------------------------------------------------------------------------
jieba>=0.42

# ----------------------------------------------------------------------------
# 繁转简（knowledge/crawlers/clean.py，TDSF 2026-08-30 C1）
#   - zh_TW 手册页（bash.1.zh_TW.html 等）检测到繁体特征字后 t2s 转简入库。
#   - 纯 Python 实现，无编译依赖；缺失时优雅降级（跳过转换，不阻塞爬取）。
# ----------------------------------------------------------------------------
opencc-python-reimplemented>=0.1.7

# ----------------------------------------------------------------------------
# SQLite 向量扩展（knowledge RAG 向量检索，可选）
#   - 缺失则向量检索禁用、仅 FTS5 关键词检索（降级可用，不阻塞启动）。
#   - 若当前 Python 版本无预编译 wheel 装不上，可注释此行（FTS5+jieba 仍可用）。
# ----------------------------------------------------------------------------
sqlite-vec>=0.1.0

# ----------------------------------------------------------------------------
# 本地嵌入模型推理（knowledge RAG 语义向量，可选）
#   - fastembed（Qdrant，ONNX）：BGE-small-zh-v1.5 本地推理，零外部 API。
#     缺失时嵌入降级 hash 伪向量（FTS5 仍可用，语义检索无效）。
#   - 模型首次使用需下载（HF_ENDPOINT=https://hf-mirror.com 走国内镜像，
#     缓存于 sidecar/data/models/）；离线环境预下载后即可用。
# ----------------------------------------------------------------------------
fastembed>=0.8.0

# ----------------------------------------------------------------------------
# 向量检索（T-P1-07 ground tool，T-P3-02 ChromaDB）
# ----------------------------------------------------------------------------
chromadb>=0.5.0

# ----------------------------------------------------------------------------
# SQLite（Python 标准库已内置 sqlite3，无需安装）
# 用于 T-P1-03 Project Service 单一写入器（WAL 模式 + 写租约）
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# 异步支持
# ----------------------------------------------------------------------------
asyncio-atexit>=1.0  # Python 3.13 异步清理

# ----------------------------------------------------------------------------
# Strands Agents SDK（现役 Agent 后端）
# -----------------------------------------------------------------------------
# Apache-2.0 协议，AWS 开源 Agent 框架，由 strands_backend/ 适配层封装。
# - 默认后端：未设置 TDSF_AGENT_BACKEND 时 main.py 注入 Strands 适配层
# - 未安装或激活失败时显式报告不可用，不回退旧 Agent
# - **版本为什么写死**：本应用贴上了 SDK 的半公开面（Agent(retry_strategy=...) /
#   ModelRetryStrategy / vended_plugins.context_offloader），历史上宽 pin 已经
#   造成过真实故障（`Agent(max_iterations=)` 在 1.50.2 被移除），而 #77 实测
#   到"门禁 1.53 / 桌面 1.50.2 / 安装包 1.56"三份运行时并存、连打包目录里都混着
#   两份 dist-info。应用不是库，装哪一版必须写死。
# - 唯一真值是 `STRANDS_RUNTIME_VERSION`；升级时同时改这两处并跑
#   `strands_backend/tests/test_runtime_parity.py`，再重跑 e2e（含审批/教学链路）。
# - 安装后还需配置 LLM provider（OpenAI/Anthropic/Bedrock 等），
#   通过 strands_backend.adapter 的 strands_model 参数注入
# -----------------------------------------------------------------------------
strands-agents==1.53.0

# ----------------------------------------------------------------------------
# 测试（pytest）
# ----------------------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0

# ----------------------------------------------------------------------------
# 开发工具（可选，仅开发环境）
# ----------------------------------------------------------------------------
# ruff>=0.6.0         # Lint + Format
# mypy>=1.10          # 类型检查
# ipython>=8.0        # 交互式调试
